/**
 * useGLTFAsset — DEPRECATED / UNUSED
 *
 * This hook is no longer used. The hero 3D scene was replaced with a
 * lightweight React code-window component (HeroScene.tsx) that doesn't
 * require Three.js GLTF loading.
 *
 * Kept as a placeholder to avoid filesystem changes — safe to delete.
 */
export function useGLTFAsset(_path: string): null {
  return null;
}
